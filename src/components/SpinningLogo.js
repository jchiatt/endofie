import React from 'react';
import styled, { keyframes } from 'styled-components';
import '../styles/spin.css';

export default function SpinningLogo() {
    return <img src={'./logo.png'} className="spin-img" />;
}